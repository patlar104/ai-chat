---
phase: 01-foundation-scaffold
plan: 05
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt
  - app/src/main/kotlin/com/example/aicompanion/MainActivity.kt
autonomous: true
requirements:
  - SCAF-05

must_haves:
  truths:
    - "AppNavHost composable exists in :app and wires routes to all 7 feature screens"
    - "Each feature route constant (VOICE_ROUTE, CHAT_ROUTE, etc.) is imported from its feature module — not redeclared inline"
    - "MainActivity calls setContent { AppNavHost() } so the nav host renders at launch"
    - "The start destination is VOICE_ROUTE"
    - "Navigating to any of the 7 feature routes renders that module's placeholder screen"
  artifacts:
    - path: "app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt"
      provides: "Single navigation host routing to all 7 feature screens"
      contains: "VOICE_ROUTE"
    - path: "app/src/main/kotlin/com/example/aicompanion/MainActivity.kt"
      provides: "Updated MainActivity with setContent calling AppNavHost"
      contains: "AppNavHost()"
  key_links:
    - from: "app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt"
      to: "feature/voice/src/main/kotlin/com/example/aicompanion/feature/voice/VoiceScreen.kt"
      via: "import of VOICE_ROUTE constant and VoiceScreen composable"
      pattern: "import com.example.aicompanion.feature.voice"
    - from: "app/src/main/kotlin/com/example/aicompanion/MainActivity.kt"
      to: "app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt"
      via: "setContent { AppNavHost() } call"
      pattern: "AppNavHost\\(\\)"
---

<objective>
Create the AppNavHost composable in :app that wires Navigation Compose 2.9.7 to all 7 feature placeholder screens. Update MainActivity to call setContent { AppNavHost() }.

Purpose: SCAF-05 requires a navigation host that routes to all feature screens. This plan creates the single wiring point where :app assembles all feature screens via their route constants. Route constants live in their feature modules — AppNavHost imports them, preserving module ownership.

Output: AppNavHost.kt with 7 composable destinations. Updated MainActivity.kt calling AppNavHost.
</objective>

<execution_context>
@/Users/patricklarocque/Desktop/project/ai-chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patricklarocque/Desktop/project/ai-chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-scaffold/01-RESEARCH.md
@.planning/phases/01-foundation-scaffold/01-02-SUMMARY.md
@.planning/phases/01-foundation-scaffold/01-04-SUMMARY.md

<interfaces>
<!-- Route constants exported by feature modules (from Plan 02): -->
<!-- com.example.aicompanion.feature.voice.VOICE_ROUTE = "voice" -->
<!-- com.example.aicompanion.feature.voice.VoiceScreen() -->
<!-- com.example.aicompanion.feature.chat.CHAT_ROUTE = "chat" -->
<!-- com.example.aicompanion.feature.chat.ChatScreen() -->
<!-- com.example.aicompanion.feature.avatar.AVATAR_ROUTE = "avatar" -->
<!-- com.example.aicompanion.feature.avatar.AvatarScreen() -->
<!-- com.example.aicompanion.feature.memory.MEMORY_ROUTE = "memory" -->
<!-- com.example.aicompanion.feature.memory.MemoryScreen() -->
<!-- com.example.aicompanion.feature.homecontrol.HOME_CONTROL_ROUTE = "homecontrol" -->
<!-- com.example.aicompanion.feature.homecontrol.HomeControlScreen() -->
<!-- com.example.aicompanion.feature.tasks.TASKS_ROUTE = "tasks" -->
<!-- com.example.aicompanion.feature.tasks.TasksScreen() -->
<!-- com.example.aicompanion.feature.settings.SETTINGS_ROUTE = "settings" -->
<!-- com.example.aicompanion.feature.settings.SettingsScreen() -->

<!-- Navigation Compose 2.9.7 API: -->
<!-- NavHost(navController, startDestination) { composable(route) { content } } -->
<!-- rememberNavController() -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppNavHost and update MainActivity</name>
  <files>
    app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt,
    app/src/main/kotlin/com/example/aicompanion/MainActivity.kt
  </files>
  <read_first>
    - app/src/main/kotlin/com/example/aicompanion/MainActivity.kt (current state from Plan 04 — contains @AndroidEntryPoint; add setContent)
    - feature/voice/src/main/kotlin/com/example/aicompanion/feature/voice/VoiceScreen.kt (confirm VOICE_ROUTE constant name and VoiceScreen function signature)
    - feature/settings/src/main/kotlin/com/example/aicompanion/feature/settings/SettingsScreen.kt (confirm SETTINGS_ROUTE constant)
    - .planning/phases/01-foundation-scaffold/01-RESEARCH.md (Pattern 4: AppNavHost with Route Constants)
  </read_first>
  <action>
Create one new file and update one existing file.

**File 1: `app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt`** (new file):

```kotlin
package com.example.aicompanion.navigation

import androidx.compose.runtime.Composable
import androidx.navigation.NavHostController
import androidx.navigation.compose.NavHost
import androidx.navigation.compose.composable
import androidx.navigation.compose.rememberNavController
import com.example.aicompanion.feature.avatar.AVATAR_ROUTE
import com.example.aicompanion.feature.avatar.AvatarScreen
import com.example.aicompanion.feature.chat.CHAT_ROUTE
import com.example.aicompanion.feature.chat.ChatScreen
import com.example.aicompanion.feature.homecontrol.HOME_CONTROL_ROUTE
import com.example.aicompanion.feature.homecontrol.HomeControlScreen
import com.example.aicompanion.feature.memory.MEMORY_ROUTE
import com.example.aicompanion.feature.memory.MemoryScreen
import com.example.aicompanion.feature.settings.SETTINGS_ROUTE
import com.example.aicompanion.feature.settings.SettingsScreen
import com.example.aicompanion.feature.tasks.TASKS_ROUTE
import com.example.aicompanion.feature.tasks.TasksScreen
import com.example.aicompanion.feature.voice.VOICE_ROUTE
import com.example.aicompanion.feature.voice.VoiceScreen

@Composable
fun AppNavHost(
    navController: NavHostController = rememberNavController(),
) {
    NavHost(
        navController = navController,
        startDestination = VOICE_ROUTE,
    ) {
        composable(VOICE_ROUTE) { VoiceScreen() }
        composable(CHAT_ROUTE) { ChatScreen() }
        composable(AVATAR_ROUTE) { AvatarScreen() }
        composable(MEMORY_ROUTE) { MemoryScreen() }
        composable(HOME_CONTROL_ROUTE) { HomeControlScreen() }
        composable(TASKS_ROUTE) { TasksScreen() }
        composable(SETTINGS_ROUTE) { SettingsScreen() }
    }
}
```

**File 2: `app/src/main/kotlin/com/example/aicompanion/MainActivity.kt`** — read current content first, then update:

The current state (from Plan 04) has `@AndroidEntryPoint` and `enableEdgeToEdge()` with a comment about AppNavHost. Replace the full file with:

```kotlin
package com.example.aicompanion

import android.os.Bundle
import androidx.activity.ComponentActivity
import androidx.activity.compose.setContent
import androidx.activity.enableEdgeToEdge
import dagger.hilt.android.AndroidEntryPoint
import com.example.aicompanion.navigation.AppNavHost

@AndroidEntryPoint
class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        enableEdgeToEdge()
        setContent {
            AppNavHost()
        }
    }
}
```

Note: An AICompanionTheme wrapper is intentionally omitted in Phase 1 — the :core:ui module has only a placeholder Theme.kt. Theme will be applied in Phase 2 when the design system is implemented. The app will compile and run with the system default theme for now.

CRITICAL: The route constants MUST be imported from their feature modules — do NOT copy the string literals ("voice", "chat", etc.) directly into AppNavHost. The feature module owns its route string.
  </action>
  <verify>
    <automated>grep -c "VOICE_ROUTE" /Users/patricklarocque/Desktop/project/ai-chat/app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt && grep -c "SETTINGS_ROUTE" /Users/patricklarocque/Desktop/project/ai-chat/app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt && grep -c "AppNavHost()" /Users/patricklarocque/Desktop/project/ai-chat/app/src/main/kotlin/com/example/aicompanion/MainActivity.kt && grep -c "import com.example.aicompanion.feature" /Users/patricklarocque/Desktop/project/ai-chat/app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt</automated>
  </verify>
  <acceptance_criteria>
    - AppNavHost.kt exists at app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt
    - AppNavHost.kt imports all 7 route constants from their respective feature packages (verify with grep for `import com.example.aicompanion.feature.voice.VOICE_ROUTE`, etc.)
    - AppNavHost.kt contains exactly 7 `composable(` calls
    - AppNavHost.kt contains `startDestination = VOICE_ROUTE`
    - AppNavHost.kt does NOT contain any string literal routes like `"voice"` or `"settings"` (routes come from imported constants only)
    - MainActivity.kt contains `setContent {` and `AppNavHost()`
    - MainActivity.kt contains `@AndroidEntryPoint` (not removed from Plan 04 output)
    - MainActivity.kt imports `com.example.aicompanion.navigation.AppNavHost`
  </acceptance_criteria>
  <done>AppNavHost routes to all 7 feature placeholder screens using imported route constants. MainActivity calls setContent { AppNavHost() } from within @AndroidEntryPoint Activity. All 7 feature routes are wired.</done>
</task>

</tasks>

<verification>
After task completes:

1. `grep "composable(" app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt | wc -l` — must output 7
2. `grep "startDestination = VOICE_ROUTE" app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt` — must match
3. `grep "setContent" app/src/main/kotlin/com/example/aicompanion/MainActivity.kt` — must match
4. `grep -n '"voice"\|"chat"\|"avatar"\|"memory"\|"homecontrol"\|"tasks"\|"settings"' app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt` — must return no output (routes are constants, not string literals)
5. `grep "@AndroidEntryPoint" app/src/main/kotlin/com/example/aicompanion/MainActivity.kt` — must still be present (from Plan 04)
</verification>

<success_criteria>
- AppNavHost.kt exists with NavHost wiring 7 feature route composables using imported constants
- No inline route string literals in AppNavHost.kt
- MainActivity.kt setContent { AppNavHost() } call present
- @AndroidEntryPoint annotation preserved on MainActivity
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-scaffold/01-05-SUMMARY.md` documenting:
- AppNavHost.kt location and routes wired
- Confirmation: routes are imported constants (not inline strings)
- Start destination: VOICE_ROUTE
- Theme wrapper deferred: AICompanionTheme will wrap AppNavHost in Phase 2
</output>
