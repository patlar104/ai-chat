---
phase: 01-foundation-scaffold
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/build.gradle.kts
  - app/src/main/AndroidManifest.xml
  - app/src/main/kotlin/com/example/aicompanion/AICompanionApp.kt
  - app/src/main/kotlin/com/example/aicompanion/MainActivity.kt
  - core/ui/build.gradle.kts
  - core/ui/src/main/AndroidManifest.xml
  - core/ui/src/main/kotlin/com/example/aicompanion/core/ui/Theme.kt
  - core/domain/build.gradle.kts
  - core/domain/src/main/AndroidManifest.xml
  - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/CoreDomain.kt
  - core/data/build.gradle.kts
  - core/data/src/main/AndroidManifest.xml
  - core/data/src/main/kotlin/com/example/aicompanion/core/data/CoreData.kt
  - core/network/build.gradle.kts
  - core/network/src/main/AndroidManifest.xml
  - core/network/src/main/kotlin/com/example/aicompanion/core/network/CoreNetwork.kt
  - core/audio/build.gradle.kts
  - core/audio/src/main/AndroidManifest.xml
  - core/audio/src/main/kotlin/com/example/aicompanion/core/audio/CoreAudio.kt
  - core/ai/build.gradle.kts
  - core/ai/src/main/AndroidManifest.xml
  - core/ai/src/main/kotlin/com/example/aicompanion/core/ai/CoreAi.kt
  - core/automation/build.gradle.kts
  - core/automation/src/main/AndroidManifest.xml
  - core/automation/src/main/kotlin/com/example/aicompanion/core/automation/CoreAutomation.kt
  - feature/voice/build.gradle.kts
  - feature/voice/src/main/AndroidManifest.xml
  - feature/voice/src/main/kotlin/com/example/aicompanion/feature/voice/VoiceScreen.kt
  - feature/chat/build.gradle.kts
  - feature/chat/src/main/AndroidManifest.xml
  - feature/chat/src/main/kotlin/com/example/aicompanion/feature/chat/ChatScreen.kt
  - feature/avatar/build.gradle.kts
  - feature/avatar/src/main/AndroidManifest.xml
  - feature/avatar/src/main/kotlin/com/example/aicompanion/feature/avatar/AvatarScreen.kt
  - feature/memory/build.gradle.kts
  - feature/memory/src/main/AndroidManifest.xml
  - feature/memory/src/main/kotlin/com/example/aicompanion/feature/memory/MemoryScreen.kt
  - feature/homecontrol/build.gradle.kts
  - feature/homecontrol/src/main/AndroidManifest.xml
  - feature/homecontrol/src/main/kotlin/com/example/aicompanion/feature/homecontrol/HomeControlScreen.kt
  - feature/tasks/build.gradle.kts
  - feature/tasks/src/main/AndroidManifest.xml
  - feature/tasks/src/main/kotlin/com/example/aicompanion/feature/tasks/TasksScreen.kt
  - feature/settings/build.gradle.kts
  - feature/settings/src/main/AndroidManifest.xml
  - feature/settings/src/main/kotlin/com/example/aicompanion/feature/settings/SettingsScreen.kt
autonomous: true
requirements:
  - SCAF-01

must_haves:
  truths:
    - "All 15 module directories exist with a compilable build.gradle.kts and at least one .kt placeholder file"
    - "Each feature module exposes a route constant (e.g., VOICE_ROUTE = \"voice\") and a placeholder @Composable screen"
    - "Each core module has a placeholder Kotlin file establishing its namespace"
    - "The :app module has AICompanionApp.kt and MainActivity.kt as empty compilable shells (no Hilt annotations yet — those come in Plan 04)"
    - "./gradlew projects lists all 15 modules"
  artifacts:
    - path: "app/build.gradle.kts"
      provides: ":app module build config"
      contains: "com.android.application"
    - path: "feature/voice/src/main/kotlin/com/example/aicompanion/feature/voice/VoiceScreen.kt"
      provides: "Feature screen placeholder with route constant"
      contains: "VOICE_ROUTE"
    - path: "core/domain/build.gradle.kts"
      provides: ":core:domain module build config"
      contains: "com.example.aicompanion.core.domain"
    - path: "feature/settings/src/main/kotlin/com/example/aicompanion/feature/settings/SettingsScreen.kt"
      provides: "Last feature module placeholder"
      contains: "SETTINGS_ROUTE"
  key_links:
    - from: "feature/*/build.gradle.kts"
      to: "gradle/libs.versions.toml"
      via: "alias(libs.plugins.*) references"
      pattern: "alias\\(libs\\.plugins\\.android\\.library\\)"
    - from: "app/build.gradle.kts"
      to: "gradle/libs.versions.toml"
      via: "dependency catalog references"
      pattern: "implementation\\(libs\\."
---

<objective>
Scaffold all 15 Android modules with compilable build.gradle.kts files and placeholder Kotlin source files. This creates the full module structure that everything in Phases 2–6 will build on top of.

Purpose: Phase 1's primary deliverable (SCAF-01) is a compilable 15-module project. This plan creates that project skeleton. No feature logic — only the structural skeleton that proves the module graph compiles.

Output: 15 module directories, each with a valid build.gradle.kts referencing the version catalog, an AndroidManifest.xml, and one placeholder .kt file with the correct namespace.
</objective>

<execution_context>
@/Users/patricklarocque/Desktop/project/ai-chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patricklarocque/Desktop/project/ai-chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-scaffold/01-RESEARCH.md
@.planning/phases/01-foundation-scaffold/01-01-SUMMARY.md

<interfaces>
<!-- Version catalog aliases available from Plan 01 output. Use exactly these names. -->
<!-- From gradle/libs.versions.toml [plugins]: -->
<!-- android-application, android-library, kotlin-android, compose-compiler, kotlin-serialization, hilt, ksp, detekt, ktlint -->

<!-- From gradle/libs.versions.toml [libraries] (key ones for scaffold): -->
<!-- compose-bom, compose-ui, compose-material3, compose-ui-tooling-preview -->
<!-- hilt-android, hilt-compiler (ksp), hilt-navigation-compose -->
<!-- activity-compose, lifecycle-viewmodel-compose, lifecycle-runtime-compose -->
<!-- navigation-compose -->
<!-- coroutines-android, coroutines-core -->
<!-- timber -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold :app module and 8 core modules</name>
  <files>
    app/build.gradle.kts,
    app/src/main/AndroidManifest.xml,
    app/src/main/kotlin/com/example/aicompanion/AICompanionApp.kt,
    app/src/main/kotlin/com/example/aicompanion/MainActivity.kt,
    core/ui/build.gradle.kts,
    core/ui/src/main/AndroidManifest.xml,
    core/ui/src/main/kotlin/com/example/aicompanion/core/ui/Theme.kt,
    core/domain/build.gradle.kts,
    core/domain/src/main/AndroidManifest.xml,
    core/domain/src/main/kotlin/com/example/aicompanion/core/domain/CoreDomain.kt,
    core/data/build.gradle.kts,
    core/data/src/main/AndroidManifest.xml,
    core/data/src/main/kotlin/com/example/aicompanion/core/data/CoreData.kt,
    core/network/build.gradle.kts,
    core/network/src/main/AndroidManifest.xml,
    core/network/src/main/kotlin/com/example/aicompanion/core/network/CoreNetwork.kt,
    core/audio/build.gradle.kts,
    core/audio/src/main/AndroidManifest.xml,
    core/audio/src/main/kotlin/com/example/aicompanion/core/audio/CoreAudio.kt,
    core/ai/build.gradle.kts,
    core/ai/src/main/AndroidManifest.xml,
    core/ai/src/main/kotlin/com/example/aicompanion/core/ai/CoreAi.kt,
    core/automation/build.gradle.kts,
    core/automation/src/main/AndroidManifest.xml,
    core/automation/src/main/kotlin/com/example/aicompanion/core/automation/CoreAutomation.kt
  </files>
  <read_first>
    - gradle/libs.versions.toml (confirm plugin and library alias names before writing build.gradle.kts files)
    - settings.gradle.kts (confirm all module paths are listed — task creates the matching directories)
    - .planning/phases/01-foundation-scaffold/01-RESEARCH.md (Pattern 1: Minimal Module Stub, Pattern 3: Hilt App-Level Wiring for app module shape)
  </read_first>
  <action>
Create all directories and files listed. Use these exact patterns:

---

**app/build.gradle.kts:**
```kotlin
plugins {
    alias(libs.plugins.android.application)
    alias(libs.plugins.kotlin.android)
    alias(libs.plugins.compose.compiler)
    alias(libs.plugins.hilt)
    alias(libs.plugins.ksp)
}

android {
    namespace = "com.example.aicompanion"
    compileSdk = 35

    defaultConfig {
        applicationId = "com.example.aicompanion"
        minSdk = 26
        targetSdk = 35
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            isMinifyEnabled = false
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    buildFeatures {
        compose = true
    }
}

dependencies {
    implementation(project(":core:domain"))
    implementation(project(":core:ui"))
    implementation(project(":core:data"))
    implementation(project(":core:network"))
    implementation(project(":core:audio"))
    implementation(project(":core:ai"))
    implementation(project(":core:automation"))

    implementation(project(":feature:voice"))
    implementation(project(":feature:chat"))
    implementation(project(":feature:avatar"))
    implementation(project(":feature:memory"))
    implementation(project(":feature:homecontrol"))
    implementation(project(":feature:tasks"))
    implementation(project(":feature:settings"))

    implementation(libs.hilt.android)
    ksp(libs.hilt.compiler)

    implementation(platform(libs.compose.bom))
    implementation(libs.compose.ui)
    implementation(libs.compose.material3)
    implementation(libs.compose.ui.tooling.preview)
    debugImplementation(libs.compose.ui.tooling)

    implementation(libs.activity.compose)
    implementation(libs.lifecycle.runtime.ktx)
    implementation(libs.navigation.compose)
    implementation(libs.hilt.navigation.compose)

    implementation(libs.timber)
    implementation(libs.coroutines.android)
}
```

**app/src/main/AndroidManifest.xml:**
```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:name=".AICompanionApp"
        android:label="AI Companion"
        android:theme="@style/Theme.AppCompat">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
```

**app/src/main/kotlin/com/example/aicompanion/AICompanionApp.kt:**
```kotlin
package com.example.aicompanion

import android.app.Application

// Placeholder — @HiltAndroidApp and Timber.plant() added in Plan 04 (Hilt wiring)
class AICompanionApp : Application()
```

**app/src/main/kotlin/com/example/aicompanion/MainActivity.kt:**
```kotlin
package com.example.aicompanion

import androidx.activity.ComponentActivity

// Placeholder — @AndroidEntryPoint, setContent{}, and AppNavHost() added in Plans 04 and 05
class MainActivity : ComponentActivity()
```

---

For each core module, use this pattern (substituting module-specific values):

**core/domain/build.gradle.kts:**
```kotlin
plugins {
    alias(libs.plugins.android.library)
    alias(libs.plugins.kotlin.android)
}

android {
    namespace = "com.example.aicompanion.core.domain"
    compileSdk = 35
    defaultConfig { minSdk = 26 }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions { jvmTarget = "17" }
}

dependencies {
    implementation(libs.coroutines.core)
    testImplementation(libs.junit)
    testImplementation(libs.mockk)
    testImplementation(libs.coroutines.test)
    testImplementation(libs.turbine)
}
```

**core/domain/src/main/AndroidManifest.xml:**
```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.aicompanion.core.domain" />
```

**core/domain/src/main/kotlin/com/example/aicompanion/core/domain/CoreDomain.kt:**
```kotlin
package com.example.aicompanion.core.domain

// Placeholder — error model and domain interfaces added in Plan 06
```

Apply the same 3-file pattern to the remaining core modules, substituting namespace and package:

| Module | namespace | package suffix | Placeholder class comment |
|--------|-----------|----------------|--------------------------|
| `:core:ui` | `com.example.aicompanion.core.ui` | `core.ui` | Theme.kt — shared design tokens added later |
| `:core:data` | `com.example.aicompanion.core.data` | `core.data` | Room + DataStore wiring added Phase 2 |
| `:core:network` | `com.example.aicompanion.core.network` | `core.network` | OkHttp + Retrofit added Phase 2 |
| `:core:audio` | `com.example.aicompanion.core.audio` | `core.audio` | SpeechRecognizer/TTS abstractions added Phase 2 |
| `:core:ai` | `com.example.aicompanion.core.ai` | `core.ai` | AI router added Phase 3 |
| `:core:automation` | `com.example.aicompanion.core.automation` | `core.automation` | WorkManager workers added Phase 4 |

For `:core:ui` — its build.gradle.kts adds Compose since it hosts shared UI components:
```kotlin
plugins {
    alias(libs.plugins.android.library)
    alias(libs.plugins.kotlin.android)
    alias(libs.plugins.compose.compiler)
}

android {
    namespace = "com.example.aicompanion.core.ui"
    compileSdk = 35
    defaultConfig { minSdk = 26 }
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions { jvmTarget = "17" }
    buildFeatures { compose = true }
}

dependencies {
    implementation(platform(libs.compose.bom))
    implementation(libs.compose.ui)
    implementation(libs.compose.material3)
    implementation(libs.compose.ui.tooling.preview)
    debugImplementation(libs.compose.ui.tooling)
    testImplementation(libs.junit)
}
```

`:core:ui` placeholder file: `core/ui/src/main/kotlin/com/example/aicompanion/core/ui/Theme.kt`
```kotlin
package com.example.aicompanion.core.ui

// Placeholder — AICompanionTheme composable added in Phase 2
```

CRITICAL: Do NOT add `kapt` to any module. Do NOT use `kotlinCompilerExtensionVersion`. Do NOT add `composeOptions` blocks.
  </action>
  <verify>
    <automated>ls /Users/patricklarocque/Desktop/project/ai-chat/app/build.gradle.kts /Users/patricklarocque/Desktop/project/ai-chat/core/domain/build.gradle.kts /Users/patricklarocque/Desktop/project/ai-chat/core/ui/build.gradle.kts /Users/patricklarocque/Desktop/project/ai-chat/core/data/build.gradle.kts /Users/patricklarocque/Desktop/project/ai-chat/core/network/build.gradle.kts /Users/patricklarocque/Desktop/project/ai-chat/core/audio/build.gradle.kts /Users/patricklarocque/Desktop/project/ai-chat/core/ai/build.gradle.kts /Users/patricklarocque/Desktop/project/ai-chat/core/automation/build.gradle.kts && grep -r "kapt" /Users/patricklarocque/Desktop/project/ai-chat/app/build.gradle.kts /Users/patricklarocque/Desktop/project/ai-chat/core/ || echo "no kapt found (correct)"</automated>
  </verify>
  <acceptance_criteria>
    - app/build.gradle.kts exists and contains `com.example.aicompanion` as namespace
    - app/build.gradle.kts contains `ksp(libs.hilt.compiler)` (NOT kapt)
    - app/build.gradle.kts contains `implementation(platform(libs.compose.bom))`
    - app/src/main/AndroidManifest.xml exists and contains `android:name=".AICompanionApp"`
    - All 8 core module directories created: core/ui, core/domain, core/data, core/network, core/audio, core/ai, core/automation
    - Each core module has: build.gradle.kts, src/main/AndroidManifest.xml, and one .kt placeholder file
    - No build.gradle.kts in core/ or app/ contains `kapt` or `kotlinCompilerExtensionVersion`
    - `grep -r "composeOptions" app/ core/` returns no output
  </acceptance_criteria>
  <done>:app and 8 core modules scaffolded with compilable build.gradle.kts files, correct namespaces, and placeholder Kotlin files. No deprecated patterns (kapt, kotlinCompilerExtensionVersion, composeOptions) present.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold 7 feature modules</name>
  <files>
    feature/voice/build.gradle.kts,
    feature/voice/src/main/AndroidManifest.xml,
    feature/voice/src/main/kotlin/com/example/aicompanion/feature/voice/VoiceScreen.kt,
    feature/chat/build.gradle.kts,
    feature/chat/src/main/AndroidManifest.xml,
    feature/chat/src/main/kotlin/com/example/aicompanion/feature/chat/ChatScreen.kt,
    feature/avatar/build.gradle.kts,
    feature/avatar/src/main/AndroidManifest.xml,
    feature/avatar/src/main/kotlin/com/example/aicompanion/feature/avatar/AvatarScreen.kt,
    feature/memory/build.gradle.kts,
    feature/memory/src/main/AndroidManifest.xml,
    feature/memory/src/main/kotlin/com/example/aicompanion/feature/memory/MemoryScreen.kt,
    feature/homecontrol/build.gradle.kts,
    feature/homecontrol/src/main/AndroidManifest.xml,
    feature/homecontrol/src/main/kotlin/com/example/aicompanion/feature/homecontrol/HomeControlScreen.kt,
    feature/tasks/build.gradle.kts,
    feature/tasks/src/main/AndroidManifest.xml,
    feature/tasks/src/main/kotlin/com/example/aicompanion/feature/tasks/TasksScreen.kt,
    feature/settings/build.gradle.kts,
    feature/settings/src/main/AndroidManifest.xml,
    feature/settings/src/main/kotlin/com/example/aicompanion/feature/settings/SettingsScreen.kt
  </files>
  <read_first>
    - gradle/libs.versions.toml (confirm alias names for feature module dependencies)
    - build.gradle.kts (confirm feature→feature ban is installed — this task must NOT violate it)
    - .planning/phases/01-foundation-scaffold/01-RESEARCH.md (Pattern 1: Minimal Module Stub — exact feature module build.gradle.kts template)
  </read_first>
  <action>
Create all 7 feature modules. Each module gets exactly: one build.gradle.kts, one AndroidManifest.xml, and one Screen.kt placeholder with a route constant and @Composable function.

**Feature module build.gradle.kts template** (apply to all 7 feature modules, substituting namespace):

```kotlin
plugins {
    alias(libs.plugins.android.library)
    alias(libs.plugins.kotlin.android)
    alias(libs.plugins.compose.compiler)
    alias(libs.plugins.hilt)
    alias(libs.plugins.ksp)
}

android {
    namespace = "com.example.aicompanion.feature.MODULENAME"
    compileSdk = 35
    defaultConfig { minSdk = 26 }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions { jvmTarget = "17" }
    buildFeatures { compose = true }
}

dependencies {
    // Feature modules ONLY depend on core modules — NEVER on other feature modules
    implementation(project(":core:domain"))
    implementation(project(":core:ui"))

    implementation(libs.hilt.android)
    ksp(libs.hilt.compiler)
    implementation(libs.hilt.navigation.compose)

    implementation(platform(libs.compose.bom))
    implementation(libs.compose.ui)
    implementation(libs.compose.material3)
    implementation(libs.compose.ui.tooling.preview)
    debugImplementation(libs.compose.ui.tooling)

    implementation(libs.lifecycle.viewmodel.compose)
    implementation(libs.lifecycle.runtime.compose)
    implementation(libs.coroutines.android)

    testImplementation(libs.junit)
    testImplementation(libs.mockk)
    testImplementation(libs.coroutines.test)
    testImplementation(libs.turbine)
}
```

Apply to all 7 features with these namespaces:
- `:feature:voice` → `com.example.aicompanion.feature.voice`
- `:feature:chat` → `com.example.aicompanion.feature.chat`
- `:feature:avatar` → `com.example.aicompanion.feature.avatar`
- `:feature:memory` → `com.example.aicompanion.feature.memory`
- `:feature:homecontrol` → `com.example.aicompanion.feature.homecontrol`
- `:feature:tasks` → `com.example.aicompanion.feature.tasks`
- `:feature:settings` → `com.example.aicompanion.feature.settings`

**Feature module AndroidManifest.xml** (same for all 7, substitute package):
```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.aicompanion.feature.MODULENAME" />
```

**Feature Screen placeholder** — create one Screen.kt per module:

`feature/voice/src/main/kotlin/com/example/aicompanion/feature/voice/VoiceScreen.kt`:
```kotlin
package com.example.aicompanion.feature.voice

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

const val VOICE_ROUTE = "voice"

@Composable
fun VoiceScreen() {
    Text(text = "Voice — placeholder")
}
```

`feature/chat/src/main/kotlin/com/example/aicompanion/feature/chat/ChatScreen.kt`:
```kotlin
package com.example.aicompanion.feature.chat

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

const val CHAT_ROUTE = "chat"

@Composable
fun ChatScreen() {
    Text(text = "Chat — placeholder")
}
```

`feature/avatar/src/main/kotlin/com/example/aicompanion/feature/avatar/AvatarScreen.kt`:
```kotlin
package com.example.aicompanion.feature.avatar

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

const val AVATAR_ROUTE = "avatar"

@Composable
fun AvatarScreen() {
    Text(text = "Avatar — placeholder")
}
```

`feature/memory/src/main/kotlin/com/example/aicompanion/feature/memory/MemoryScreen.kt`:
```kotlin
package com.example.aicompanion.feature.memory

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

const val MEMORY_ROUTE = "memory"

@Composable
fun MemoryScreen() {
    Text(text = "Memory — placeholder")
}
```

`feature/homecontrol/src/main/kotlin/com/example/aicompanion/feature/homecontrol/HomeControlScreen.kt`:
```kotlin
package com.example.aicompanion.feature.homecontrol

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

const val HOME_CONTROL_ROUTE = "homecontrol"

@Composable
fun HomeControlScreen() {
    Text(text = "Home Control — placeholder")
}
```

`feature/tasks/src/main/kotlin/com/example/aicompanion/feature/tasks/TasksScreen.kt`:
```kotlin
package com.example.aicompanion.feature.tasks

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

const val TASKS_ROUTE = "tasks"

@Composable
fun TasksScreen() {
    Text(text = "Tasks — placeholder")
}
```

`feature/settings/src/main/kotlin/com/example/aicompanion/feature/settings/SettingsScreen.kt`:
```kotlin
package com.example.aicompanion.feature.settings

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

const val SETTINGS_ROUTE = "settings"

@Composable
fun SettingsScreen() {
    Text(text = "Settings — placeholder")
}
```

CRITICAL constraints:
- No feature module may have `implementation(project(":feature:*"))` — this violates the architectural ban and will cause a build failure at configuration time
- Use `ksp(libs.hilt.compiler)` NOT `kapt(...)` for all Hilt annotation processing
- Do NOT add `composeOptions { kotlinCompilerExtensionVersion = ... }` — Compose compiler is handled by the plugin
  </action>
  <verify>
    <automated>ls /Users/patricklarocque/Desktop/project/ai-chat/feature/voice/build.gradle.kts /Users/patricklarocque/Desktop/project/ai-chat/feature/settings/build.gradle.kts /Users/patricklarocque/Desktop/project/ai-chat/feature/homecontrol/build.gradle.kts && grep -rn "project(\":feature:" /Users/patricklarocque/Desktop/project/ai-chat/feature/*/build.gradle.kts && echo "VIOLATION FOUND" || echo "No feature-to-feature deps (correct)"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 feature module directories created: feature/voice, feature/chat, feature/avatar, feature/memory, feature/homecontrol, feature/tasks, feature/settings
    - Each feature module has: build.gradle.kts, src/main/AndroidManifest.xml, and one Screen.kt with a route constant and @Composable function
    - Each Screen.kt contains a `const val *_ROUTE = "..."` constant (e.g., `VOICE_ROUTE`, `CHAT_ROUTE`, etc.)
    - `grep -rn "project(\":feature:" feature/*/build.gradle.kts` returns no output (no feature→feature deps)
    - `grep -rn "kapt" feature/*/build.gradle.kts` returns no output
    - `grep -rn "kotlinCompilerExtensionVersion" feature/*/build.gradle.kts` returns no output
    - feature/voice/src/main/kotlin/com/example/aicompanion/feature/voice/VoiceScreen.kt contains `const val VOICE_ROUTE = "voice"`
    - feature/settings/src/main/kotlin/com/example/aicompanion/feature/settings/SettingsScreen.kt contains `const val SETTINGS_ROUTE = "settings"`
  </acceptance_criteria>
  <done>All 7 feature modules scaffolded with correct namespaces, route constants, placeholder @Composable screens, and no feature→feature dependencies. All 15 modules now exist on disk matching the settings.gradle.kts declarations.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Count modules: `find . -name "build.gradle.kts" | grep -v "^./build.gradle.kts" | wc -l` should output 15
2. Verify no feature→feature deps: `grep -rn "project(\":feature:" feature/*/build.gradle.kts` must return no output
3. Verify route constants: `grep -rn "const val.*_ROUTE" feature/` must show 7 route constants
4. Verify no kapt: `grep -rn "kapt" app/ core/ feature/` must return no output
5. Run `./gradlew projects` — must list all 15 modules (requires JDK 17 and Gradle 9.3.1)
</verification>

<success_criteria>
- All 15 module directories created with build.gradle.kts, AndroidManifest.xml, and placeholder .kt file
- Each feature module has a route constant (VOICE_ROUTE, CHAT_ROUTE, AVATAR_ROUTE, MEMORY_ROUTE, HOME_CONTROL_ROUTE, TASKS_ROUTE, SETTINGS_ROUTE)
- No feature→feature dependencies in any build.gradle.kts
- No kapt, no kotlinCompilerExtensionVersion, no composeOptions blocks
- ./gradlew projects lists all 15 modules
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-scaffold/01-02-SUMMARY.md` documenting:
- All 15 modules created (list them with their placeholder file paths)
- Confirmation: no feature→feature dependencies present
- Route constants exported by each feature module
- Any issues encountered with version catalog aliases
</output>
