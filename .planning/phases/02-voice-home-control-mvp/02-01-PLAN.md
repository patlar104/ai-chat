---
phase: 02-voice-home-control-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/VoiceState.kt
  - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/ParsedIntent.kt
  - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/Message.kt
  - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/AuditLogEntry.kt
  - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/Alias.kt
  - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/Reminder.kt
  - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/repository/MessageRepository.kt
  - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/repository/AuditLogRepository.kt
  - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/repository/AliasRepository.kt
  - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/repository/ReminderRepository.kt
  - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/repository/HomeAssistantRepository.kt
  - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/repository/SettingsRepository.kt
  - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/di/DispatcherModule.kt
  - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/di/MainDispatcher.kt
  - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/di/IoDispatcher.kt
autonomous: true
requirements:
  - VOIC-02
  - CONV-03
  - CONV-04
  - HOME-03
  - HOME-06
  - TASK-01

must_haves:
  truths:
    - "All domain models are pure Kotlin with no Android imports"
    - "Repository interfaces define the full contract for data, network, and settings access"
    - "VoiceState sealed interface has all 6 states matching VOIC-02"
    - "ParsedIntent sealed interface covers all 4 deterministic parser categories"
    - "Dispatcher qualifiers enable testable coroutine injection"
  artifacts:
    - path: "core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/VoiceState.kt"
      provides: "Voice state machine types"
      contains: "sealed interface VoiceState"
    - path: "core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/ParsedIntent.kt"
      provides: "Command parser output types"
      contains: "sealed interface ParsedIntent"
    - path: "core/domain/src/main/kotlin/com/example/aicompanion/core/domain/repository/MessageRepository.kt"
      provides: "Conversation persistence contract"
      contains: "interface MessageRepository"
    - path: "core/domain/src/main/kotlin/com/example/aicompanion/core/domain/di/DispatcherModule.kt"
      provides: "Hilt dispatcher bindings"
      contains: "@Provides @MainDispatcher"
  key_links:
    - from: "core/domain/model/VoiceState.kt"
      to: "core/domain/error/AppError.kt"
      via: "Error state wraps AppError"
      pattern: "AppError"
---

<objective>
Define all pure Kotlin domain models, repository interfaces, and dispatcher qualifiers that every other Phase 2 module depends on.

Purpose: This is the "interface contract" plan. All downstream plans (data layer, audio layer, network layer, feature ViewModels) import these types. Building them first prevents the scavenger-hunt anti-pattern where executors must explore the codebase to discover contracts.

Output: ~15 Kotlin files in core/domain with zero Android imports (except the Hilt annotation on DispatcherModule).
</objective>

<execution_context>
@/Users/patricklarocque/Desktop/project/ai-chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patricklarocque/Desktop/project/ai-chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-voice-home-control-mvp/02-CONTEXT.md
@.planning/phases/02-voice-home-control-mvp/02-RESEARCH.md

<interfaces>
<!-- Existing types this plan must integrate with -->

From core/domain/error/AppError.kt:
```kotlin
sealed class AppError(override val message: String) : Exception(message) {
    data class Network(val cause: Throwable) : AppError("Network error: ${cause.message}")
    data class Storage(val cause: Throwable) : AppError("Storage error: ${cause.message}")
    data class AudioPipeline(val reason: String) : AppError("Audio error: $reason")
    data class HomeAssistant(val reason: String) : AppError("Home Assistant error: $reason")
    data object Unknown : AppError("Unknown error")
}
```

From core/domain/logging/Logger.kt:
```kotlin
interface Logger {
    fun d(tag: String, message: String)
    fun e(tag: String, message: String, throwable: Throwable? = null)
    fun w(tag: String, message: String)
    fun i(tag: String, message: String)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create domain models and sealed interfaces</name>
  <read_first>
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/error/AppError.kt
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/CoreDomain.kt
    - core/domain/build.gradle.kts
    - .planning/phases/02-voice-home-control-mvp/02-RESEARCH.md (Pattern 1: MVI Voice State Machine, Pattern 7: Deterministic Command Parser, Pattern 8: Room Schema Design sections)
  </read_first>
  <files>
    core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/VoiceState.kt
    core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/ParsedIntent.kt
    core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/Message.kt
    core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/AuditLogEntry.kt
    core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/Alias.kt
    core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/Reminder.kt
  </files>
  <action>
Create 6 model files in `core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/`. Package: `com.example.aicompanion.core.domain.model`. ALL files must be pure Kotlin — zero `android.*` imports.

**VoiceState.kt** — MVI state + events + UI effects:
```kotlin
sealed interface VoiceState {
    data object Idle : VoiceState
    data object Listening : VoiceState
    data object Transcribing : VoiceState
    data class Processing(val transcript: String) : VoiceState
    data class Speaking(val reply: String) : VoiceState
    data class Error(val error: AppError) : VoiceState
}

sealed interface VoiceEvent {
    data object MicTapped : VoiceEvent
    data object TtsDone : VoiceEvent
    data class TranscriptReceived(val text: String) : VoiceEvent
    data class RecognitionError(val code: Int) : VoiceEvent
    data class ReplyReady(val text: String) : VoiceEvent
    data class CommandError(val error: AppError) : VoiceEvent
}

sealed interface VoiceUiEffect {
    data class ShowToast(val message: String) : VoiceUiEffect
    data object Vibrate : VoiceUiEffect
}
```
Import `AppError` from `com.example.aicompanion.core.domain.error`.

**ParsedIntent.kt** — Deterministic parser output:
```kotlin
sealed interface ParsedIntent {
    data class HomeControl(
        val entityAlias: String,
        val action: HomeAction,
        val params: Map<String, Any> = emptyMap(),
    ) : ParsedIntent

    data class Routine(val routineAlias: String) : ParsedIntent

    data class CreateReminder(
        val description: String,
        val triggerTimeMs: Long,
    ) : ParsedIntent

    data class LocalQuery(val queryType: QueryType) : ParsedIntent
    data object Unknown : ParsedIntent
}

enum class HomeAction { TURN_ON, TURN_OFF, SET_BRIGHTNESS, SET_TEMPERATURE }
enum class QueryType { CURRENT_TIME, LIST_REMINDERS }
```

**Message.kt** — Domain model for conversation messages:
```kotlin
data class Message(
    val id: Long = 0,
    val role: MessageRole,
    val content: String,
    val sourceType: SourceType,
    val timestampMs: Long,
    val sessionId: String,
)

enum class MessageRole { USER, ASSISTANT }
enum class SourceType { DETERMINISTIC, UNKNOWN }
```

**AuditLogEntry.kt** — Domain model for HA command audit:
```kotlin
data class AuditLogEntry(
    val id: Long = 0,
    val command: String,
    val domain: String,
    val service: String,
    val entityId: String,
    val status: AuditStatus,
    val errorReason: String?,
    val timestampMs: Long,
)

enum class AuditStatus { SUCCESS, FAILURE }
```

**Alias.kt** — Device/room alias:
```kotlin
data class Alias(
    val id: Long = 0,
    val alias: String,
    val entityId: String,
    val domain: String,
)
```

**Reminder.kt** — Reminder model:
```kotlin
data class Reminder(
    val id: Long = 0,
    val description: String,
    val triggerTimeMs: Long,
    val status: ReminderStatus,
    val createdAtMs: Long,
)

enum class ReminderStatus { PENDING, FIRED, CANCELLED }
```

Delete the existing `CoreDomain.kt` placeholder file after creating these models.
  </action>
  <verify>
    <automated>cd /Users/patricklarocque/Desktop/project/ai-chat && grep -rn "import android" core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/ | grep -v "^$" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/VoiceState.kt contains `sealed interface VoiceState`
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/VoiceState.kt contains `data object Idle : VoiceState`
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/VoiceState.kt contains `data object Listening : VoiceState`
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/VoiceState.kt contains `data class Speaking(val reply: String) : VoiceState`
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/VoiceState.kt contains `sealed interface VoiceEvent`
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/VoiceState.kt contains `sealed interface VoiceUiEffect`
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/ParsedIntent.kt contains `sealed interface ParsedIntent`
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/ParsedIntent.kt contains `enum class HomeAction`
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/ParsedIntent.kt contains `enum class QueryType`
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/Message.kt contains `data class Message`
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/Message.kt contains `enum class SourceType`
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/AuditLogEntry.kt contains `data class AuditLogEntry`
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/Alias.kt contains `data class Alias`
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/Reminder.kt contains `data class Reminder`
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/Reminder.kt contains `enum class ReminderStatus`
    - grep for `import android` in model/ directory returns 0 matches
    - CoreDomain.kt placeholder is deleted
  </acceptance_criteria>
  <done>All 6 domain model files exist with correct sealed interfaces, data classes, and enums. Zero android.* imports in the model directory.</done>
</task>

<task type="auto">
  <name>Task 2: Create repository interfaces and dispatcher qualifiers</name>
  <read_first>
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/Message.kt (just created in Task 1)
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/AuditLogEntry.kt
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/Alias.kt
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/model/Reminder.kt
    - core/domain/src/main/kotlin/com/example/aicompanion/core/domain/di/DomainModule.kt
    - core/domain/build.gradle.kts
  </read_first>
  <files>
    core/domain/src/main/kotlin/com/example/aicompanion/core/domain/repository/MessageRepository.kt
    core/domain/src/main/kotlin/com/example/aicompanion/core/domain/repository/AuditLogRepository.kt
    core/domain/src/main/kotlin/com/example/aicompanion/core/domain/repository/AliasRepository.kt
    core/domain/src/main/kotlin/com/example/aicompanion/core/domain/repository/ReminderRepository.kt
    core/domain/src/main/kotlin/com/example/aicompanion/core/domain/repository/HomeAssistantRepository.kt
    core/domain/src/main/kotlin/com/example/aicompanion/core/domain/repository/SettingsRepository.kt
    core/domain/src/main/kotlin/com/example/aicompanion/core/domain/di/DispatcherModule.kt
    core/domain/src/main/kotlin/com/example/aicompanion/core/domain/di/MainDispatcher.kt
    core/domain/src/main/kotlin/com/example/aicompanion/core/domain/di/IoDispatcher.kt
  </files>
  <action>
Create repository interfaces and dispatcher qualifiers. Package: `com.example.aicompanion.core.domain.repository` and `com.example.aicompanion.core.domain.di`.

**MessageRepository.kt:**
```kotlin
interface MessageRepository {
    fun observeAll(): Flow<List<Message>>
    suspend fun insert(message: Message): Long
    suspend fun getRecentMessages(sinceMs: Long, limit: Int = 20): List<Message>
}
```
Import `Flow` from `kotlinx.coroutines.flow`, `Message` from the model package.

**AuditLogRepository.kt:**
```kotlin
interface AuditLogRepository {
    fun observeRecent(limit: Int = 50): Flow<List<AuditLogEntry>>
    suspend fun insert(entry: AuditLogEntry): Long
}
```

**AliasRepository.kt:**
```kotlin
interface AliasRepository {
    fun observeAll(): Flow<List<Alias>>
    suspend fun findByAlias(alias: String): Alias?
    suspend fun insert(alias: Alias): Long
    suspend fun update(alias: Alias)
    suspend fun delete(id: Long)
}
```

**ReminderRepository.kt:**
```kotlin
interface ReminderRepository {
    fun observePending(): Flow<List<Reminder>>
    suspend fun insert(reminder: Reminder): Long
    suspend fun updateStatus(id: Long, status: ReminderStatus)
    suspend fun getAllPending(): List<Reminder>
}
```

**HomeAssistantRepository.kt:**
```kotlin
interface HomeAssistantRepository {
    suspend fun callService(domain: String, service: String, entityId: String, params: Map<String, Any> = emptyMap()): Result<Unit>
}
```
Return `kotlin.Result<Unit>` — success or failure with exception.

**SettingsRepository.kt:**
```kotlin
interface SettingsRepository {
    val haServerUrl: Flow<String>
    val haAccessToken: Flow<String?>
    val ttsVoiceName: Flow<String?>
    val privacyModeEnabled: Flow<Boolean>
    val backgroundAutomationEnabled: Flow<Boolean>
    suspend fun setHaServerUrl(url: String)
    suspend fun setHaAccessToken(token: String)
    suspend fun clearHaAccessToken()
    suspend fun setTtsVoiceName(name: String)
    suspend fun setPrivacyModeEnabled(enabled: Boolean)
    suspend fun setBackgroundAutomationEnabled(enabled: Boolean)
}
```

**MainDispatcher.kt:**
```kotlin
@Qualifier
@Retention(AnnotationRetention.BINARY)
annotation class MainDispatcher
```
Import `javax.inject.Qualifier`.

**IoDispatcher.kt:**
```kotlin
@Qualifier
@Retention(AnnotationRetention.BINARY)
annotation class IoDispatcher
```

**DispatcherModule.kt:**
```kotlin
@Module
@InstallIn(SingletonComponent::class)
object DispatcherModule {
    @Provides
    @MainDispatcher
    fun provideMainDispatcher(): CoroutineDispatcher = Dispatchers.Main

    @Provides
    @IoDispatcher
    fun provideIoDispatcher(): CoroutineDispatcher = Dispatchers.IO
}
```
Imports: `dagger.Module`, `dagger.Provides`, `dagger.hilt.InstallIn`, `dagger.hilt.components.SingletonComponent`, `kotlinx.coroutines.CoroutineDispatcher`, `kotlinx.coroutines.Dispatchers`.

Ensure `core/domain/build.gradle.kts` already has `coroutines-core` dependency (it does per Phase 1). No build.gradle changes needed.
  </action>
  <verify>
    <automated>cd /Users/patricklarocque/Desktop/project/ai-chat && ls core/domain/src/main/kotlin/com/example/aicompanion/core/domain/repository/*.kt | wc -l | tr -d ' ' && ls core/domain/src/main/kotlin/com/example/aicompanion/core/domain/di/*.kt | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - 6 repository interface files exist in core/domain/src/.../repository/
    - core/domain/src/.../repository/MessageRepository.kt contains `interface MessageRepository`
    - core/domain/src/.../repository/MessageRepository.kt contains `fun observeAll(): Flow<List<Message>>`
    - core/domain/src/.../repository/AuditLogRepository.kt contains `interface AuditLogRepository`
    - core/domain/src/.../repository/AliasRepository.kt contains `interface AliasRepository`
    - core/domain/src/.../repository/AliasRepository.kt contains `suspend fun findByAlias(alias: String): Alias?`
    - core/domain/src/.../repository/ReminderRepository.kt contains `interface ReminderRepository`
    - core/domain/src/.../repository/ReminderRepository.kt contains `suspend fun getAllPending(): List<Reminder>`
    - core/domain/src/.../repository/HomeAssistantRepository.kt contains `interface HomeAssistantRepository`
    - core/domain/src/.../repository/HomeAssistantRepository.kt contains `suspend fun callService(`
    - core/domain/src/.../repository/SettingsRepository.kt contains `interface SettingsRepository`
    - core/domain/src/.../repository/SettingsRepository.kt contains `val haServerUrl: Flow<String>`
    - core/domain/src/.../di/DispatcherModule.kt contains `@Provides` and `@MainDispatcher`
    - core/domain/src/.../di/MainDispatcher.kt contains `annotation class MainDispatcher`
    - core/domain/src/.../di/IoDispatcher.kt contains `annotation class IoDispatcher`
    - 3 files in di/ directory (DispatcherModule.kt, MainDispatcher.kt, IoDispatcher.kt) plus existing DomainModule.kt
  </acceptance_criteria>
  <done>All 6 repository interfaces and 3 dispatcher qualifier/module files exist. Interfaces use domain models and kotlinx.coroutines.flow.Flow — no Android imports in repository interfaces.</done>
</task>

</tasks>

<verification>
- All files compile as pure Kotlin (no android.* imports in model/ or repository/)
- VoiceState has exactly 6 states: Idle, Listening, Transcribing, Processing, Speaking, Error
- ParsedIntent has exactly 5 variants: HomeControl, Routine, CreateReminder, LocalQuery, Unknown
- All repository interfaces use suspend functions and Flow return types
- DispatcherModule provides both @MainDispatcher and @IoDispatcher
</verification>

<success_criteria>
- 6 domain model files exist with all sealed interfaces, enums, and data classes
- 6 repository interface files exist defining full data/network/settings contracts
- 3 dispatcher DI files exist (qualifiers + module)
- Zero android.* imports in model/ and repository/ directories
- CoreDomain.kt placeholder is removed
</success_criteria>

<output>
After completion, create `.planning/phases/02-voice-home-control-mvp/02-01-SUMMARY.md`
</output>
