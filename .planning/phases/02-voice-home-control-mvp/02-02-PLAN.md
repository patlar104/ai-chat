---
phase: 02-voice-home-control-mvp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/ui/src/main/kotlin/com/example/aicompanion/core/ui/Theme.kt
  - core/ui/src/main/kotlin/com/example/aicompanion/core/ui/Spacing.kt
  - core/ui/src/main/kotlin/com/example/aicompanion/core/ui/Color.kt
  - app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt
  - app/src/main/kotlin/com/example/aicompanion/navigation/BottomNavBar.kt
  - app/src/main/kotlin/com/example/aicompanion/MainActivity.kt
autonomous: true
requirements:
  - VOIC-02

must_haves:
  truths:
    - "App uses dark color scheme with #111318 background and #4F8EF7 accent"
    - "Bottom navigation bar shows 5 destinations: Voice, Chat, Home, Tasks, Settings"
    - "Navigation between all 5 main screens works via bottom nav taps"
    - "Spacing object provides xs through 3xl tokens as defined in UI-SPEC"
  artifacts:
    - path: "core/ui/src/main/kotlin/com/example/aicompanion/core/ui/Theme.kt"
      provides: "AICompanionTheme composable with dark color scheme and custom typography"
      contains: "fun AICompanionTheme"
    - path: "core/ui/src/main/kotlin/com/example/aicompanion/core/ui/Spacing.kt"
      provides: "Spacing tokens (xs=4dp through 3xl=64dp)"
      contains: "object Spacing"
    - path: "app/src/main/kotlin/com/example/aicompanion/navigation/BottomNavBar.kt"
      provides: "Material 3 NavigationBar with 5 destinations"
      contains: "NavigationBar"
  key_links:
    - from: "MainActivity.kt"
      to: "Theme.kt"
      via: "AICompanionTheme wraps Scaffold"
      pattern: "AICompanionTheme"
    - from: "MainActivity.kt"
      to: "BottomNavBar.kt"
      via: "Scaffold bottomBar parameter"
      pattern: "bottomBar"
---

<objective>
Create the AICompanionTheme (dark color scheme, typography, spacing tokens) and wire the bottom navigation bar into the app shell with Scaffold.

Purpose: Every feature screen depends on the theme for consistent styling, and every screen needs the bottom nav bar to be accessible. This plan establishes the visual foundation and navigation shell that all subsequent UI plans build inside.

Output: Theme composable, spacing object, color tokens, bottom nav bar, and updated MainActivity/AppNavHost using Scaffold.
</objective>

<execution_context>
@/Users/patricklarocque/Desktop/project/ai-chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patricklarocque/Desktop/project/ai-chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-voice-home-control-mvp/02-CONTEXT.md
@.planning/phases/02-voice-home-control-mvp/02-UI-SPEC.md

<interfaces>
From app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt:
```kotlin
@Composable
fun AppNavHost(navController: NavHostController = rememberNavController()) {
    NavHost(navController = navController, startDestination = VOICE_ROUTE) {
        composable(VOICE_ROUTE) { VoiceScreen() }
        composable(CHAT_ROUTE) { ChatScreen() }
        composable(AVATAR_ROUTE) { AvatarScreen() }
        composable(MEMORY_ROUTE) { MemoryScreen() }
        composable(HOME_CONTROL_ROUTE) { HomeControlScreen() }
        composable(TASKS_ROUTE) { TasksScreen() }
        composable(SETTINGS_ROUTE) { SettingsScreen() }
    }
}
```

Route constants:
- VOICE_ROUTE = "voice"
- CHAT_ROUTE = "chat"
- HOME_CONTROL_ROUTE = "homecontrol"
- TASKS_ROUTE = "tasks"
- SETTINGS_ROUTE = "settings"

From core/ui/src/main/kotlin/com/example/aicompanion/core/ui/Theme.kt:
```kotlin
// Placeholder — AICompanionTheme composable added in Phase 2
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AICompanionTheme with dark color scheme, typography, and spacing tokens</name>
  <read_first>
    - core/ui/src/main/kotlin/com/example/aicompanion/core/ui/Theme.kt
    - core/ui/build.gradle.kts
    - .planning/phases/02-voice-home-control-mvp/02-UI-SPEC.md
  </read_first>
  <files>
    core/ui/src/main/kotlin/com/example/aicompanion/core/ui/Theme.kt
    core/ui/src/main/kotlin/com/example/aicompanion/core/ui/Spacing.kt
    core/ui/src/main/kotlin/com/example/aicompanion/core/ui/Color.kt
  </files>
  <action>
Replace the Theme.kt placeholder and create Spacing.kt and Color.kt. Package: `com.example.aicompanion.core.ui`.

**Color.kt** — Color tokens from UI-SPEC:
```kotlin
package com.example.aicompanion.core.ui

import androidx.compose.ui.graphics.Color

object AppColors {
    val Background = Color(0xFF111318)
    val Surface = Color(0xFF111318)
    val SurfaceContainer = Color(0xFF1E2029)
    val SurfaceVariant = Color(0xFF1E2029)
    val Primary = Color(0xFF4F8EF7)        // Accent
    val OnPrimary = Color(0xFFFFFFFF)
    val OnSurface = Color(0xFFE2E4EC)
    val OnSurfaceVariant = Color(0xFF8A8FA8)
    val Error = Color(0xFFE05353)          // Destructive
    val OnError = Color(0xFFFFFFFF)
    val AuditSuccess = Color(0xFF4CAF50)   // Semantic-only for audit log
}
```

**Spacing.kt** — Spacing tokens from UI-SPEC:
```kotlin
package com.example.aicompanion.core.ui

import androidx.compose.ui.unit.Dp
import androidx.compose.ui.unit.dp

object Spacing {
    val xs: Dp = 4.dp
    val sm: Dp = 8.dp
    val md: Dp = 16.dp
    val lg: Dp = 24.dp
    val xl: Dp = 32.dp
    val xxl: Dp = 48.dp
    val xxxl: Dp = 64.dp
}
```

**Theme.kt** — AICompanionTheme composable:
```kotlin
package com.example.aicompanion.core.ui

import androidx.compose.material3.MaterialTheme
import androidx.compose.material3.Typography
import androidx.compose.material3.darkColorScheme
import androidx.compose.runtime.Composable
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp

private val DarkColorScheme = darkColorScheme(
    background = AppColors.Background,
    surface = AppColors.Surface,
    surfaceContainer = AppColors.SurfaceContainer,
    surfaceVariant = AppColors.SurfaceVariant,
    primary = AppColors.Primary,
    onPrimary = AppColors.OnPrimary,
    onSurface = AppColors.OnSurface,
    onSurfaceVariant = AppColors.OnSurfaceVariant,
    error = AppColors.Error,
    onError = AppColors.OnError,
)

private val AppTypography = Typography(
    bodyLarge = TextStyle(
        fontSize = 16.sp,
        fontWeight = FontWeight.Normal,
        lineHeight = 24.sp,  // 16 * 1.5
    ),
    labelMedium = TextStyle(
        fontSize = 14.sp,
        fontWeight = FontWeight.Normal,
        lineHeight = 19.6.sp,  // 14 * 1.4
    ),
    headlineSmall = TextStyle(
        fontSize = 20.sp,
        fontWeight = FontWeight.SemiBold,
        lineHeight = 24.sp,  // 20 * 1.2
    ),
    displaySmall = TextStyle(
        fontSize = 28.sp,
        fontWeight = FontWeight.SemiBold,
        lineHeight = 30.8.sp,  // 28 * 1.1
    ),
)

@Composable
fun AICompanionTheme(content: @Composable () -> Unit) {
    MaterialTheme(
        colorScheme = DarkColorScheme,
        typography = AppTypography,
        content = content,
    )
}
```

Mapping from UI-SPEC roles to Material 3 typography roles:
- Body (16sp/400) -> bodyLarge
- Label (14sp/400) -> labelMedium
- Heading (20sp/600) -> headlineSmall
- Display (28sp/600) -> displaySmall
  </action>
  <verify>
    <automated>cd /Users/patricklarocque/Desktop/project/ai-chat && grep -c "fun AICompanionTheme" core/ui/src/main/kotlin/com/example/aicompanion/core/ui/Theme.kt && grep -c "object Spacing" core/ui/src/main/kotlin/com/example/aicompanion/core/ui/Spacing.kt && grep -c "object AppColors" core/ui/src/main/kotlin/com/example/aicompanion/core/ui/Color.kt</automated>
  </verify>
  <acceptance_criteria>
    - Theme.kt contains `fun AICompanionTheme(content: @Composable () -> Unit)`
    - Theme.kt contains `darkColorScheme(`
    - Theme.kt contains `background = AppColors.Background`
    - Theme.kt contains `primary = AppColors.Primary`
    - Theme.kt contains `error = AppColors.Error`
    - Theme.kt contains `Typography(`
    - Theme.kt contains `fontSize = 28.sp` (Display role)
    - Theme.kt contains `FontWeight.SemiBold`
    - Color.kt contains `val Background = Color(0xFF111318)`
    - Color.kt contains `val Primary = Color(0xFF4F8EF7)`
    - Color.kt contains `val Error = Color(0xFFE05353)`
    - Color.kt contains `val AuditSuccess = Color(0xFF4CAF50)`
    - Spacing.kt contains `val xs: Dp = 4.dp`
    - Spacing.kt contains `val md: Dp = 16.dp`
    - Spacing.kt contains `val xxxl: Dp = 64.dp`
    - No placeholder comment remains in Theme.kt
  </acceptance_criteria>
  <done>AICompanionTheme provides dark color scheme with all UI-SPEC color tokens, 4-role typography, and Spacing object with 7 dp tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Wire bottom NavigationBar into Scaffold and update AppNavHost</name>
  <read_first>
    - app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt
    - app/src/main/kotlin/com/example/aicompanion/MainActivity.kt
    - app/build.gradle.kts
    - .planning/phases/02-voice-home-control-mvp/02-UI-SPEC.md (Bottom NavigationBar section)
  </read_first>
  <files>
    app/src/main/kotlin/com/example/aicompanion/navigation/BottomNavBar.kt
    app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt
    app/src/main/kotlin/com/example/aicompanion/MainActivity.kt
  </files>
  <action>
Create BottomNavBar composable and update AppNavHost + MainActivity to use Scaffold with bottom navigation.

**BottomNavBar.kt** — Package: `com.example.aicompanion.navigation`:
```kotlin
package com.example.aicompanion.navigation

import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.filled.Chat
import androidx.compose.material.icons.filled.Home
import androidx.compose.material.icons.filled.Mic
import androidx.compose.material.icons.filled.Settings
import androidx.compose.material.icons.outlined.Chat
import androidx.compose.material.icons.outlined.Home
import androidx.compose.material.icons.outlined.Mic
import androidx.compose.material.icons.outlined.Settings
import androidx.compose.material3.Icon
import androidx.compose.material3.NavigationBar
import androidx.compose.material3.NavigationBarItem
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.graphics.vector.ImageVector
import androidx.navigation.NavController
import androidx.navigation.compose.currentBackStackEntryAsState

// Use Alarm icon (material-icons-extended) for Tasks
import androidx.compose.material.icons.filled.Alarm
import androidx.compose.material.icons.outlined.Alarm

data class BottomNavDestination(
    val route: String,
    val label: String,
    val selectedIcon: ImageVector,
    val unselectedIcon: ImageVector,
    val contentDescription: String,
)

val bottomNavDestinations = listOf(
    BottomNavDestination("voice", "Voice", Icons.Filled.Mic, Icons.Outlined.Mic, "Voice"),
    BottomNavDestination("chat", "Chat", Icons.Filled.Chat, Icons.Outlined.Chat, "Chat"),
    BottomNavDestination("homecontrol", "Home", Icons.Filled.Home, Icons.Outlined.Home, "Home Control"),
    BottomNavDestination("tasks", "Tasks", Icons.Filled.Alarm, Icons.Outlined.Alarm, "Tasks"),
    BottomNavDestination("settings", "Settings", Icons.Filled.Settings, Icons.Outlined.Settings, "Settings"),
)

@Composable
fun BottomNavBar(navController: NavController) {
    val currentEntry = navController.currentBackStackEntryAsState()
    val currentRoute = currentEntry.value?.destination?.route

    NavigationBar {
        bottomNavDestinations.forEach { destination ->
            val selected = currentRoute == destination.route
            NavigationBarItem(
                selected = selected,
                onClick = {
                    if (currentRoute != destination.route) {
                        navController.navigate(destination.route) {
                            popUpTo(navController.graph.startDestinationId) { saveState = true }
                            launchSingleTop = true
                            restoreState = true
                        }
                    }
                },
                icon = {
                    Icon(
                        imageVector = if (selected) destination.selectedIcon else destination.unselectedIcon,
                        contentDescription = destination.contentDescription,
                    )
                },
                label = { Text(destination.label) },
            )
        }
    }
}
```

NOTE: The `material-icons-extended` dependency must be added to `app/build.gradle.kts`:
```kotlin
implementation("androidx.compose.material:material-icons-extended")
```
This is provided via compose-bom so no version needed. Add this line to the app module's dependencies block. Also add it to `core/ui/build.gradle.kts` if feature modules need icons directly.

**Update AppNavHost.kt** — Change signature to accept `navController` as a required parameter (caller manages it now), keep composable routes unchanged:
```kotlin
@Composable
fun AppNavHost(
    navController: NavHostController,
    modifier: Modifier = Modifier,
) {
    NavHost(
        navController = navController,
        startDestination = VOICE_ROUTE,
        modifier = modifier,
    ) {
        // Keep all 7 composable() calls unchanged
        composable(VOICE_ROUTE) { VoiceScreen() }
        composable(CHAT_ROUTE) { ChatScreen() }
        composable(AVATAR_ROUTE) { AvatarScreen() }
        composable(MEMORY_ROUTE) { MemoryScreen() }
        composable(HOME_CONTROL_ROUTE) { HomeControlScreen() }
        composable(TASKS_ROUTE) { TasksScreen() }
        composable(SETTINGS_ROUTE) { SettingsScreen() }
    }
}
```
Add `import androidx.compose.ui.Modifier` to the imports.

**Update MainActivity.kt** — Wrap content in AICompanionTheme and Scaffold:
```kotlin
package com.example.aicompanion

import android.os.Bundle
import androidx.activity.ComponentActivity
import androidx.activity.compose.setContent
import androidx.compose.foundation.layout.padding
import androidx.compose.material3.Scaffold
import androidx.compose.ui.Modifier
import androidx.navigation.compose.rememberNavController
import com.example.aicompanion.core.ui.AICompanionTheme
import com.example.aicompanion.navigation.AppNavHost
import com.example.aicompanion.navigation.BottomNavBar
import dagger.hilt.android.AndroidEntryPoint

@AndroidEntryPoint
class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContent {
            AICompanionTheme {
                val navController = rememberNavController()
                Scaffold(
                    bottomBar = { BottomNavBar(navController = navController) },
                ) { innerPadding ->
                    AppNavHost(
                        navController = navController,
                        modifier = Modifier.padding(innerPadding),
                    )
                }
            }
        }
    }
}
```
Ensure `@AndroidEntryPoint` annotation is present (it should be from Phase 1).
  </action>
  <verify>
    <automated>cd /Users/patricklarocque/Desktop/project/ai-chat && grep -c "NavigationBar" app/src/main/kotlin/com/example/aicompanion/navigation/BottomNavBar.kt && grep -c "AICompanionTheme" app/src/main/kotlin/com/example/aicompanion/MainActivity.kt && grep -c "Scaffold" app/src/main/kotlin/com/example/aicompanion/MainActivity.kt</automated>
  </verify>
  <acceptance_criteria>
    - BottomNavBar.kt exists and contains `NavigationBar`
    - BottomNavBar.kt contains 5 NavigationBarItem entries (Voice, Chat, Home, Tasks, Settings)
    - BottomNavBar.kt contains `bottomNavDestinations` list with 5 entries
    - BottomNavBar.kt contains `Icons.Filled.Mic` and `Icons.Outlined.Mic`
    - BottomNavBar.kt contains `contentDescription = "Voice"`
    - BottomNavBar.kt contains `popUpTo(navController.graph.startDestinationId)`
    - AppNavHost.kt contains `navController: NavHostController` as required parameter (not defaulted)
    - AppNavHost.kt contains `modifier: Modifier = Modifier`
    - AppNavHost.kt still contains all 7 composable() calls
    - MainActivity.kt contains `AICompanionTheme`
    - MainActivity.kt contains `Scaffold(`
    - MainActivity.kt contains `bottomBar = { BottomNavBar(`
    - MainActivity.kt contains `Modifier.padding(innerPadding)`
    - MainActivity.kt contains `@AndroidEntryPoint`
    - app/build.gradle.kts contains `material-icons-extended` dependency
  </acceptance_criteria>
  <done>Bottom navigation bar with 5 destinations is wired into Scaffold. MainActivity uses AICompanionTheme. All screens are navigable via bottom nav with proper back-stack management.</done>
</task>

</tasks>

<verification>
- AICompanionTheme wraps the entire app content
- Dark color scheme colors match UI-SPEC: #111318, #1E2029, #4F8EF7, #E05353
- Typography has 4 roles at correct sizes: 16sp, 14sp, 20sp, 28sp
- Spacing object has 7 tokens: 4dp, 8dp, 16dp, 24dp, 32dp, 48dp, 64dp
- NavigationBar has exactly 5 items with correct labels and icons
- Navigation popUpTo prevents back-stack buildup
</verification>

<success_criteria>
- Theme.kt provides AICompanionTheme with dark color scheme and custom typography
- Color.kt and Spacing.kt provide all UI-SPEC design tokens
- BottomNavBar.kt renders 5 destinations with correct icons and labels
- MainActivity.kt wraps content in AICompanionTheme + Scaffold with BottomNavBar
- AppNavHost accepts external navController and modifier for proper Scaffold integration
</success_criteria>

<output>
After completion, create `.planning/phases/02-voice-home-control-mvp/02-02-SUMMARY.md`
</output>
