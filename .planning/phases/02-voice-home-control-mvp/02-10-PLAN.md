---
phase: 02-voice-home-control-mvp
plan: 10
type: execute
wave: 5
depends_on: ["02-07", "02-08", "02-09"]
files_modified:
  - app/build.gradle.kts
autonomous: false
requirements:
  - VOIC-01
  - VOIC-02
  - VOIC-03
  - VOIC-04
  - VOIC-05
  - VOIC-06
  - TTS-01
  - TTS-02
  - TTS-03
  - CONV-01
  - CONV-02
  - CONV-04
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05
  - HOME-06
  - HOME-07
  - TASK-01
  - TASK-02
  - TASK-03
  - TASK-04
  - SETT-01
  - SETT-02
  - SETT-03
  - SETT-04

must_haves:
  truths:
    - "App compiles and runs without errors"
    - "Full voice loop works: tap -> speak -> hear reply"
    - "Bottom navigation provides access to all 5 screens"
    - "Settings connect to Home Assistant and persist"
    - "Conversation history persists across app restart"
  artifacts:
    - path: "app/build.gradle.kts"
      provides: "All module dependencies wired"
      contains: "implementation(project("
  key_links:
    - from: "app module"
      to: "all core and feature modules"
      via: "Gradle dependencies"
      pattern: "implementation.*project"
---

<objective>
Final wiring and compilation verification: ensure app module depends on all Phase 2 modules, the project compiles, and all integration points are connected.

Purpose: Catch any missing imports, circular dependencies, or Hilt graph issues before the human verification checkpoint. The executor verifies the build compiles and performs a self-check of all cross-module connections.

Output: Compilable app with all Phase 2 features integrated.
</objective>

<execution_context>
@/Users/patricklarocque/Desktop/project/ai-chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patricklarocque/Desktop/project/ai-chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-voice-home-control-mvp/02-CONTEXT.md
All prior plan SUMMARYs from Phase 2
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire app module dependencies and verify compilation</name>
  <read_first>
    - app/build.gradle.kts
    - app/src/main/kotlin/com/example/aicompanion/navigation/AppNavHost.kt
    - app/src/main/kotlin/com/example/aicompanion/MainActivity.kt
    - app/src/main/kotlin/com/example/aicompanion/AICompanionApp.kt
    - settings.gradle.kts
  </read_first>
  <files>
    app/build.gradle.kts
  </files>
  <action>
Verify `app/build.gradle.kts` has implementation dependencies on all core and feature modules needed for Phase 2. The app module should depend on:

```kotlin
// Core modules
implementation(project(":core:domain"))
implementation(project(":core:ui"))
implementation(project(":core:data"))
implementation(project(":core:audio"))
implementation(project(":core:ai"))
implementation(project(":core:network"))
implementation(project(":core:automation"))

// Feature modules
implementation(project(":feature:voice"))
implementation(project(":feature:chat"))
implementation(project(":feature:homecontrol"))
implementation(project(":feature:tasks"))
implementation(project(":feature:settings"))
implementation(project(":feature:avatar"))
implementation(project(":feature:memory"))
```

Also verify these are present:
- `libs.hilt.android` + `ksp(libs.hilt.compiler)`
- `libs.navigation.compose`
- `libs.activity.compose`
- `libs.compose.bom` platform
- `libs.timber`
- `"androidx.compose.material:material-icons-extended"`

After updating build.gradle.kts, run a compilation check:
```bash
cd /path/to/project && ./gradlew :app:compileDebugKotlin 2>&1 | tail -20
```

If compilation fails, diagnose and fix the issue. Common problems:
1. Missing imports in Kotlin files (fix the import)
2. Missing dependency in a module's build.gradle.kts (add it)
3. Hilt graph errors (ensure all @Inject constructors have matching @Provides or @Binds)
4. Room schema issues (ensure @Entity classes are properly annotated)

Fix any issues found until the build succeeds.
  </action>
  <verify>
    <automated>cd /Users/patricklarocque/Desktop/project/ai-chat && ./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/build.gradle.kts contains `implementation(project(":core:data"))`
    - app/build.gradle.kts contains `implementation(project(":core:audio"))`
    - app/build.gradle.kts contains `implementation(project(":core:ai"))`
    - app/build.gradle.kts contains `implementation(project(":core:network"))`
    - app/build.gradle.kts contains `implementation(project(":core:automation"))`
    - app/build.gradle.kts contains `implementation(project(":feature:voice"))`
    - app/build.gradle.kts contains `implementation(project(":feature:chat"))`
    - app/build.gradle.kts contains `implementation(project(":feature:homecontrol"))`
    - app/build.gradle.kts contains `implementation(project(":feature:tasks"))`
    - app/build.gradle.kts contains `implementation(project(":feature:settings"))`
    - `./gradlew :app:compileDebugKotlin` exits with BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>App module compiles successfully with all Phase 2 module dependencies wired.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 2 MVP</name>
  <files>N/A</files>
  <action>
Human verifies the complete Phase 2 MVP: voice interaction loop, home control via HA REST API, reminder scheduling, conversation history, settings screen, and bottom navigation. The app should:
1. Launch with dark theme and bottom navigation bar
2. Show VoiceScreen as the home screen with a mic button
3. Navigate between Voice, Chat, Home Control, Tasks, and Settings via bottom nav
4. Accept voice input when mic button is tapped
5. Parse spoken commands and speak TTS replies
6. Display conversation history in ChatScreen
7. Show command audit log in HomeControlScreen
8. Allow alias management in HomeControlScreen
9. Show pending reminders in TasksScreen
10. Configure HA connection and preferences in SettingsScreen

Steps to verify:
1. Build and install: `./gradlew installDebug`
2. Launch the app on a device/emulator (API 29+)
3. Verify dark theme with bottom navigation bar (5 tabs: Voice, Chat, Home, Tasks, Settings)
4. Tap the mic button -- grant microphone permission when prompted
5. Speak a command (e.g., "what time is it") -- verify TTS reply and mic button state transitions
6. Navigate to Chat tab -- verify the command and reply appear as message bubbles
7. Navigate to Settings -- enter HA URL and token, select a TTS voice, toggle switches
8. Navigate to Home Control -- verify empty audit log state and alias editor
9. Add a device alias (e.g., "living room lights" -> "light.living_room" -> domain: light)
10. Return to Voice -- say "turn on living room lights" -- verify audit log entry appears
11. Say "remind me to take medication at 8 PM" -- verify reminder appears in Tasks tab
12. Delete the reminder in Tasks -- verify confirmation dialog and removal
  </action>
  <verify>Human confirms MVP works end-to-end by typing "approved" or describing issues found.</verify>
  <done>Human approves the Phase 2 MVP as functional, or issues are documented for gap closure.</done>
</task>

</tasks>

<verification>
- App launches without crash on API 29+ device/emulator
- All 5 bottom nav destinations are reachable
- Voice loop completes: tap -> speak -> hear reply -> see in chat
- Settings persist across app restart
- Conversation history persists across app restart
</verification>

<success_criteria>
- App compiles and runs without errors
- All Phase 2 features functional end-to-end
- Human verification confirms MVP quality
</success_criteria>

<output>
After completion, create `.planning/phases/02-voice-home-control-mvp/02-10-SUMMARY.md`
</output>
