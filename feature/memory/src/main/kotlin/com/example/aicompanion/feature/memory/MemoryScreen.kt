package com.example.aicompanion.feature.memory

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

const val MEMORY_ROUTE = "memory"

@Composable
fun MemoryScreen() {
    Text(text = "Memory — placeholder")
}
