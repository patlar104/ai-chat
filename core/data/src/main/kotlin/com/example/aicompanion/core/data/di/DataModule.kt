package com.example.aicompanion.core.data.di

import dagger.Module
import dagger.hilt.InstallIn
import dagger.hilt.components.SingletonComponent

@Module
@InstallIn(SingletonComponent::class)
object DataModule {
    // Placeholder — Room database and DataStore bindings added in Phase 2+
    // Example: @Provides @Singleton fun provideDatabase(@ApplicationContext ctx: Context): AppDatabase
}
