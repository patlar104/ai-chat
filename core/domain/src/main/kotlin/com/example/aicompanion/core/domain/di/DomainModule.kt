package com.example.aicompanion.core.domain.di

import dagger.Module
import dagger.hilt.InstallIn
import dagger.hilt.components.SingletonComponent

@Module
@InstallIn(SingletonComponent::class)
object DomainModule {
    // Placeholder — repository interface bindings added in Phase 3+
    // Example: @Provides fun provideConversationRepository(...): ConversationRepository
}
